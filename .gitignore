# .NET build output
bin/
obj/
out/
publish/
*.user
*.suo
*.userprefs

# IDEs
.vs/
.vscode/
.idea/
*.swp

# Test results
TestResults/
coverage/
*.trx
*.coverage
*.coveragexml

# Tool output
.tools/
*.nupkg
*.snupkg

# OS
.DS_Store
Thumbs.db

# Secrets (defense in depth — gitleaks is the real gate)
*.pfx
*.p12
*.pem
*.key
appsettings.Local.json
.env
.env.*
!.env.example
dist/

# Agent scratch space — excluded by repo policy, not by a nested .gitignore
.superpowers/

# Claude Code — personal settings stay local; .claude/settings.json is shared
.claude/settings.local.json
