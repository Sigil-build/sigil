# Build dependencies for the documentation site only. Deliberately NOT part of
# the .NET restore: nothing in src/ or tests/ needs Python, and the locked NuGet
# restore (R23a) stays the single story for everything that ships.
#
# Pinned exactly, on the same principle as packages.lock.json: a new
# mkdocs-material release must not be able to change the published site without
# a commit saying so. This is the version the first Cloudflare Pages build
# actually resolved and served — read back from the live page's generator meta
# tag (mkdocs-1.6.1, mkdocs-material-9.7.7) rather than assumed.
mkdocs-material==9.7.7
